{
  "version": 2,
  "root": "zip",
  "buildCommand": "npm install && npm run build",
  "installCommand": "npm install",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/(.*)", "dest": "/$1" }
  ]
}